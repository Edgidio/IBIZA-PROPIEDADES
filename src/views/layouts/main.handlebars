<!DOCTYPE html>
<html lang="{{lang}}">

{{#eq rutaIF "Backend"}}
  {{> "dashboard/componentes/head" }}
{{/eq}}

{{#eq rutaIF "Frontend"}}
{{> "frontend/componentes/head" }}
{{/eq}}

{{#eq rutaIF "error_404"}}
{{> "frontend/componentes/head404" }}
{{/eq}}

 <style type="text/css">  
 html, body {  
   margin:0px;  
   height:100%;  
 }  
 </style>  

<body class="">
  <div id="google_translate_element" style="display: none;"></div>

    {{{body}}}

<script type="text/javascript">
        document.addEventListener('DOMContentLoaded', function() {
            const lang = '{{lang}}';
            if (lang === 'en') {
                setTimeout(function() {
                    var translateElement = document.getElementById('google_translate_element');
                    if (translateElement) {
                        var iframe = translateElement.querySelector('iframe');
                        if (iframe) {
                            var innerDoc = iframe.contentDocument || iframe.contentWindow.document;
                            var select = innerDoc.querySelector('select');
                            if (select) {
                                select.value = 'en';
                                select.dispatchEvent(new Event('change'));
                            }
                        }
                    }
                }, 500); // Esperar un poco para asegurarse de que el elemento se haya cargado
            }
        });
    </script>
</body>
</html>