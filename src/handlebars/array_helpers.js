// helpers.js
/* import Handlebars from 'handlebars';

Handlebars.registerHelper('eachArray', function (context, options) {
  let ret = "";
  for (let i = 1, j = context; i <= j; i++) {
    ret = ret + options.fn(i);
  }
  return ret;
}); */